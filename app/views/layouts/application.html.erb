<!DOCTYPE html>
<html>
  <head>
    <title>SWD</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <!--Since we've added the OpenStruct to the controller, our new current_user method will continue to be logged in. In order to differentiate between a User that is fully logged in, we use the Class argument to use the logged in User data. -->
    <% if current_user.is_a?(User) %>
      <%= link_to "Signout", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Sign In", new_user_session_path %>
      <%= link_to "Register", new_user_registration_path %>
    <% end %>
    <%= yield %>
    <% if session[:source] %>
      <p>Thanks for visiting from <%= session[:source] %></p>
    <% end %>
  </body>
  </html>
